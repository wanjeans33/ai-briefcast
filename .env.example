# 火山引擎豆包语音 凭据模板
# 用法：复制为 .env 并填入真实值（.env 已被 gitignore，不会提交）
#   cp .env.example .env
# scripts/doubao_tts_ws.py 会自动读取仓库根目录的 .env

VOLC_APP_ID=你的appid
VOLC_API_KEY=你的api-key
VOLC_SPEAKER=S_xxxxxxxx
# 可选，默认 seed-icl-2.0
VOLC_RESOURCE_ID=seed-icl-2.0
